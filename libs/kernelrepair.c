#include <stdlib.h>
#include <stdio.h>
#include <string.h>
/*
If you read this, im still working on it, i thout maybe i first find the arch of the traget system, 
then find the package manager, then repair the kernel via reinstalling the kernel package.
please be patient :)

*/


int main(){
    printf("%d", enumarate_arch("/sys"));
    return 0;
}

int debian_kernel_repair(){
    
    return 0;
}
